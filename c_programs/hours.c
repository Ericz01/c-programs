#include "cs50.h"
#include <stdio.h>

int main()
{
    int i, n, A;
    double T;
    n = get_int("Enter number of weeks: ");
    int weeks[n];
    for (i = 0; i < n; i++)
    {
        printf("Weeks[%d]= ", i);
        scanf("%d", &weeks[i]);
       
    }
   for (i = 0; i< n; i++)
   {
        printf("Array: %d\n", weeks[i]);
   }
   do
   {
    if 
   } while (/* condition */);
   

}