#include <stdio.h>

int main()
{
	int change, quarters = 25, dimes = 10, nickels = 5, pennies = 1, change25, change10, change5, change1;
	do
	{
		printf("Change: ");
		scanf("%d", &change);
	}
	while(change < 0);
	if (change > quarters);
	{
		while (change <= quarters)

	}
}
