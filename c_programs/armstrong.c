#include <stdio.h>

int main(void)
/*Take a anumber from the user;
 * determine whether the number is Armstrong ie sum of its characters's cubes add up to the number
 * tell the user whether the number is Armstrong or not
 */
{
	int n, i, 
}
